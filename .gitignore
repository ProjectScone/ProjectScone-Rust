reference/
# Private per-machine environment; only the reviewed template is shareable.
/.env
/.env.*
!/.env.example
/terraform/
**/.terraform/
*.tfstate
*.tfstate.*
# Local upstream application reference; never vendor it into Scone.
/supermemory/
/pipecat/
Webapp/node_modules/
Webapp/dist/
Webapp/.env*
# Local generated sessions and rejected/unused image renditions.
:memory:.ses
/Webapp/assets/scone-mark-v1.png
/Webapp/src/assets/scone-mark-v2.png
target/
*.db
.scone/
memory/raw/

# Private: the knowledge base and design docs are the proprietary part.
/memory/
docs/
bench-data/
bench-runs/
# Python packages (native engine and HTTP client)
python/*/.venv/
python/**/__pycache__/
python/*/*.egg-info/
python/*/build/
python/*/dist/
python/*/.pytest_cache/
# Codex keeps the capability ledger; it is not part of the published tree.
CAPABILITIES.md
python/*/src/*.egg-info/
python/memory/examples
